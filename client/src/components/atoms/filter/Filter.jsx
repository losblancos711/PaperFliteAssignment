import React from "react";

import { Button } from "../button/Button";

export const Filter = () => {
  return (
    <Button hasBorder>
      <span className="icon-filter"></span>
    </Button>
  );
};
